# This is a basic workflow to help you get started with Actions

name: PUSH_TO_S3

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v1
      
      # Runs a single command using the runners shell
      - name: Run a one-line script
        run :  git diff --name-only --diff-filter=AM -- ${{ github.event.before }} -- ${{ github.sha }}

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          sh execute.sh
          
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          # aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }} # if you have/need it
          aws-region: us-east-1
          S3_BUCKET: namespace3

      - name: Upload to S3
        run: |
          echo "bucket name : " S3_BUCKET
          aws s3 cp kube-example  s3://namespace3/kube-example --recursive
          #args: s3 cp config-repo.tar.gz  s3://$S3_BUCKET/config-repo.tar.gz 
    







#       - name: get changed files
#         id: getfile
#         run: |
#           echo "::set-output name=files::$(git diff --color ${{ github.event.before }} ${{ github.sha }})"

#       - name: echo output
#         run: |
#           echo ${{ steps.getfile.outputs.files }}
          
#       - name: Send mail
#         uses: dawidd6/action-send-mail@v3
#         with:
#           # Required mail server address:
#           server_address: smtp.gmail.com
#           # Required mail server port:
#           server_port: 25
#           username: srinath.goolore74@gmail.com
#           password: hrqsxcqflfbamuko
#           # Required mail subject:
#           subject: Github Actions job result
#           # Required recipients' addresses:
#           to: srinath.goolore74@gmail.com
#           # Required sender full name (address can be skipped):
#           from: Srinath Goolore # <srinath.goolore74@gmail.com>
#           # Optional plain body:
#           body: Build job of ${{github.repository}} completed successfully! ${{ steps.getfile.outputs.files }}
        
          
      
     
